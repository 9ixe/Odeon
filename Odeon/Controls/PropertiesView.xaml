<UserControl
    x:Class="Odeon.Controls.PropertiesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:strings="using:Odeon.Strings"
    d:DesignHeight="300"
    d:DesignWidth="400"
    FontFamily="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular, Segoe UI"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular, Segoe UI" />
        </Style>
        <Style x:Key="BodyTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
            <Setter Property="FontFamily" Value="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular, Segoe UI" />
        </Style>
        <Style x:Key="BodyStrongTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyStrongTextBlockStyle}">
            <Setter Property="FontFamily" Value="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular, Segoe UI" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="4,8">
        <Grid 
            Padding="16"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top">
            
            <StackPanel Spacing="12">
                <!-- Title Row -->
                <Grid Visibility="{x:Bind ViewModel.Title, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{strings:Resources Key=PropertyTitle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Title, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                </Grid>

                <!-- Length Row -->
                <Grid Visibility="{x:Bind ViewModel.Length, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{strings:Resources Key=PropertyLength}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Length, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
                </Grid>

                <!-- Resolution Row -->
                <Grid Visibility="{x:Bind ViewModel.Resolution, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{strings:Resources Key=PropertyResolution}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.Resolution, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
                </Grid>

                <!-- Bit rate Row -->
                <Grid Visibility="{x:Bind ViewModel.BitRate, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{strings:Resources Key=PropertyBitRate}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.BitRate, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
                </Grid>

                <!-- File size Row -->
                <Grid Visibility="{x:Bind ViewModel.FileSize, Mode=OneWay, Converter={StaticResource StringVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{strings:Resources Key=PropertySize}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{StaticResource BodyTextBlockStyle}" />
                    <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.FileSize, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" />
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
