<UserControl
    x:Class="Odeon.Controls.SeekBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Odeon.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:strings="using:Odeon.Strings"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    d:DesignHeight="300"
    d:DesignWidth="400"
    UseLayoutRounding="True"
    mc:Ignorable="d"
    FontFamily="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Styles/Progress.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Brushes are defined globally in App.xaml to avoid duplication -->
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="RootGrid" FlowDirection="LeftToRight">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="SeekingStates">
                <VisualState x:Name="Normal">
                    <VisualState.Setters>
                        <Setter Target="PlayerProgressBar.MinHeight" Value="5.6" />
                        <Setter Target="BufferingProgressBar.MinHeight" Value="5.6" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Seeking">
                    <VisualState.Setters>
                        <Setter Target="PlayerProgressBar.MinHeight" Value="12.6" />
                        <Setter Target="BufferingProgressBar.MinHeight" Value="12.6" />
                    </VisualState.Setters>
                </VisualState>
                <VisualStateGroup.Transitions>
                    <VisualTransition To="Seeking">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PlayerProgressBar" Storyboard.TargetProperty="MinHeight" To="12.6" Duration="0:0:0.2" EnableDependentAnimation="True">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetName="BufferingProgressBar" Storyboard.TargetProperty="MinHeight" To="12.6" Duration="0:0:0.2" EnableDependentAnimation="True">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </VisualTransition>
                    <VisualTransition To="Normal">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="PlayerProgressBar" Storyboard.TargetProperty="MinHeight" To="5.6" Duration="0:0:0.2" EnableDependentAnimation="True">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetName="BufferingProgressBar" Storyboard.TargetProperty="MinHeight" To="5.6" Duration="0:0:0.2" EnableDependentAnimation="True">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </VisualTransition>
                </VisualStateGroup.Transitions>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:ChapterProgressBar
            x:Name="PlayerProgressBar"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            AutomationProperties.AccessibilityView="Raw"
            Chapters="{x:Bind ViewModel.Chapters, Mode=OneWay}"
            CornerRadius="5"
            Foreground="#FFE81123"
            Maximum="{x:Bind SeekBarSlider.Maximum, Mode=OneWay}"
            Value="{x:Bind SeekBarSlider.Value, Mode=OneWay}"
            MinHeight="5.6"
            RenderTransformOrigin="0.5,0.5">
        </controls:ChapterProgressBar>
        <muxc:ProgressBar
            x:Name="BufferingProgressBar"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            AutomationProperties.AccessibilityView="Raw"
            Style="{StaticResource NoAnimationProgressBarStyle}"
            Foreground="#FFE81123"
            IsIndeterminate="True"
            MinHeight="5.6"
            Visibility="{x:Bind ViewModel.BufferingVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
            RenderTransformOrigin="0.5,0.5">
            <muxc:ProgressBar.Resources>
                <CornerRadius x:Key="ProgressBarCornerRadius">5</CornerRadius>
                <CornerRadius x:Key="ProgressBarTrackCornerRadius">5</CornerRadius>
            </muxc:ProgressBar.Resources>
        </muxc:ProgressBar>
        <controls:CustomSlider
            x:Name="SeekBarSlider"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            AutomationProperties.Name="{strings:Resources Key=Seek}"
            Background="Transparent"
            Foreground="Transparent"
            IsEnabled="{x:Bind ViewModel.IsSeekable, Mode=OneWay, FallbackValue=False}"
            IsKeyDownEnabled="{x:Bind ViewModel.ShouldHandleKeyDown, Mode=OneWay}"
            IsThumbToolTipEnabled="{x:Bind ViewModel.ShouldShowPreview, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
            LargeChange="10000"
            Loaded="SeekBarSlider_OnLoaded"
            Maximum="{x:Bind ViewModel.Length, Mode=OneWay, FallbackValue=0}"
            Minimum="0"
            PointerCanceled="SeekBarSlider_OnPointerExited"
            PointerEntered="SeekBarSlider_OnPointerEntered"
            PointerExited="SeekBarSlider_OnPointerExited"
            PointerWheelChanged="SeekBarSlider_OnPointerWheelChanged"
            SizeChanged="SeekBarSlider_OnSizeChanged"
            SmallChange="5000"
            Style="{StaticResource PlayerSliderStyle}"
            ThumbToolTipValueConverter="{StaticResource HumanizedDurationConverter}"
            ValueChanged="{x:Bind ViewModel.OnSeekBarValueChanged}"
            Visibility="{x:Bind ProgressOnly, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true}"
            Value="{x:Bind ViewModel.Time, Mode=OneWay}" />
    </Grid>
</UserControl>
