<ContentDialog
    x:Class="Odeon.Dialogs.PropertiesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Odeon.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:strings="using:Odeon.Strings"
    Title="{strings:Resources Key=Properties}"
    CloseButtonText="{strings:Resources Key=Close}"
    DefaultButton="Close"
    Style="{StaticResource DefaultContentDialogStyle}"
    BorderThickness="0"
    FontFamily="ms-appx:///Assets/Fonts/Linotte-Regular.otf#Linotte-Regular, Segoe UI"
    mc:Ignorable="d">

    <ContentDialog.Background>
        <AcrylicBrush
            BackgroundSource="Backdrop"
            FallbackColor="{ThemeResource SystemAltMediumColor}"
            TintColor="{ThemeResource SystemAltHighColor}"
            TintOpacity="0.8" />
    </ContentDialog.Background>

    <Grid Margin="-4,0">
        <controls:PropertiesView MinWidth="400" Media="{x:Bind Media, Mode=OneWay}" />
    </Grid>
</ContentDialog>
